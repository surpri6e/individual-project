import styled from 'styled-components/native';

export const MarathonTitle = styled.Text`
    font-size: 24px;
    font-weight: 800;
`;

export const MarathonText = styled.Text`
    font-size: 14px;
`;

export const MarathonWrapper = styled.View`
    margin: 10px 0;

    border-bottom-width: 1px;
    border-bottom-color: black;
`;
